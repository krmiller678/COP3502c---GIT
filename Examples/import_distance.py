import distance
print(f'Distance squared is {distance.math.pow(distance.distance,2)}')
x2 = 100
print(distance.math.pow(x2,2)) #when you import distance math is also imported, but module must also be referenced
